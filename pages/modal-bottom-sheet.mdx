## Installation

Add the following dependencies to your app's `build.gradle.kts` file:

```kotlin copy
dependencies {
    implementation("com.nomanr:composables:$version")
    implementation("com.nomanr:lumo:$version")
}
```

Then add the component:

```bash copy
./gradlew lumo --add ModalBottomSheet
```
